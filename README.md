# SwarLok

A music website with:
- Universal local audio import (MP3, MP4, M4A, WAV, FLAC, AAC, OGG, OPUS, and more).
- Folder import with automatic folder-based sections in the library UI.
- Optional backend sync for imported tracks.
